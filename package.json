{
  "name": "fuwari-nanoribbon-kobo",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "ふわり！ナノリボン工房 - a gentle ultramicrotomy playground for very young children",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "typecheck": "tsc --noEmit",
    "preview": "vite preview --port 4173 --host",
    "test": "vitest run",
    "audit:screens": "node tools/audit.mjs"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vite": "^5.4.10",
    "vitest": "^2.1.4"
  }
}
