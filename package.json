{
  "name": "escalator-maintenance-game",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview --host"
  },
  "devDependencies": {
    "playwright": "^1.62.1",
    "typescript": "^5.5.4",
    "vite": "^5.4.2"
  }
}
